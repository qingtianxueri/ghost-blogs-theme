<section id="ScrollBody" class="container u-p-t-2 {{#is "paged"}}margin-top{{/is}}">
	<div class="row">
		<div class="col s12 content">
			<div class="feed-entry-wrapper">
				{{#foreach posts}} {{> entry}} {{/foreach}}
			</div>

			{{#is "paged"}}
				{{"pagination"}}
			{{else}}
				<div class="pagination wow flip" style="display:none">
					<a href="#" id="pagination" class="pagination-link" mapache-page="{{pagination.pages}}" mapache-limit="{{@blog.posts_per_page}}">下一页</a>
				</div>
				<div class="pagination wow flip" style="display:none">
					<a href="#" id="prev-pagination" class="pagination-link" mapache-page="{{pagination.pages}}" mapache-limit="{{@blog.posts_per_page}}" disabled="disabled">上一页</a>
				</div>
			{{/is}}
		</div>

		{{!-- Add Sidebar --}}
		{{> "sidebar"}}

	</div>
</section>
